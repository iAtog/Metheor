const Discord = require('discord.js');

module.exports.run = async(client, msg, args) => {
  /*let server = args[0];
  if(!server)return msg.channel.send("Coloca la ip de un servidor!");
  let server2 = server.split(':');
  if(!server2[0])return msg.channel.send("Coloca la ip del servidor")
  if(!server2[1])return msg.channel.send("Coloca sus puertos!")*/
  
  
  
}

module.exports.help = {
  name: "server"
}